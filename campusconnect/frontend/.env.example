# Leave VITE_API_URL empty to run in Demo Mode (all data is local, no backend needed).
# Set it to your Express backend to run against real APIs, e.g. /api (with the Vite proxy) or https://api.example.com/api
VITE_API_URL=
